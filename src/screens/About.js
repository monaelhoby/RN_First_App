import React from 'react';
import {Card, Block, Text} from '../shared'

export default function About() {
  return (
    <Block >
      <Card>
        <Text text="About Screen" />
      </Card>
    </Block>
  );
}